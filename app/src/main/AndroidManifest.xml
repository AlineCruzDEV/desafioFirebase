<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.digitalhouse.desafiofirebase">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_desafio_firebase"
        android:supportsRtl="true"
        android:theme="@style/Theme.DesafioFirebase">
        <activity android:name=".jogo.ui.CadastraJogoActivity"></activity>
        <activity android:name=".jogo.ui.DetalheJogoActivity" />
        <activity android:name=".jogo.ui.CadastroActivity" />
        <activity android:name=".jogo.ui.LoginActivity" />
        <activity android:name=".jogo.ui.HomeActivity" />
        <activity android:name=".jogo.ui.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>